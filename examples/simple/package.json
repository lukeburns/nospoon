{
  "name": "@nospoon/simple-browser-net",
  "version": "0.1.0",
  "private": true,
  "description": "Model example: Node-style net service in the browser; static page is both UI client and virtual TCP server",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:inline": "node scripts/build.mjs --inline",
    "preview": "node scripts/preview.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.27.7",
    "esbuild-plugins-node-modules-polyfill": "^1.8.1"
  },
  "engines": {
    "node": ">=20"
  }
}
