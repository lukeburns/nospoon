'use strict'

/** Shared literals for the HTTP control plane and session mixins. */
module.exports = Object.freeze({
  SWARM_STATUS_DEBOUNCE_MS: 120,
  TOPIC_DISCOVERY_POLL_MS: 12_000,
  CONTROL_PANEL_DNS_HOST: 'nospoon'
})
